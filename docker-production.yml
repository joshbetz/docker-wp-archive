version: '2'
services:
    php:
        restart: always
        environment:
            WP_DOMAIN: betz.mx
            WP_PRODUCTION: 'true'
            WP_ADMIN_USER: josh
            WP_EMAIL: j@joshbetz.com

    nginx:
        restart: always

    mysql:
        restart: always
        volumes:
            - ".data:/var/lib/mysql"

    memcached:
        restart: always
