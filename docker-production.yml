php:
    restart: on-failure
    environment:
        WP_DOMAIN: betz.mx

nginx:
    restart: on-failure
    ports:
        - "8000:80"
    environment:
        NGINX_PORT: 8000

mysql:
    restart: on-failure
    volumes:
        - ".data:/var/lib/mysql"

memcached:
    restart: on-failure
