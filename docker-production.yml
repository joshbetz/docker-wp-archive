php:
    restart: on-failure
    environment:
        WP_DOMAIN: betz.mx

nginx:
    restart: on-failure

mysql:
    restart: on-failure
    volumes:
        - ".data:/var/lib/mysql"

memcached:
    restart: on-failure
