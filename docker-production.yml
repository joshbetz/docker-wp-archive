version: '2'
services:
    php:
        restart: always
        environment:
            WP_PRODUCTION: 'true'

    nginx:
        restart: always

    mysql:
        restart: always

    memcached:
        restart: always
